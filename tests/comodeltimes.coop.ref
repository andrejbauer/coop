resource get : unit → int
resource put : int → unit
resource get' : unit → int
resource put' : int → unit
resource tick : unit → int
val state : {get, put} ⇒ {} @ int = <runner>
val ticker : {tick} ⇒ {} @ int = <runner>
val c : {get, put, tick} ⇒ {} @ int × int = <runner>
val demo1 : (int × int × int) × (int × int) = ((10, 0, 1), (3, 2))
val demo2 : (int × int) × (int × int) = ((2, 5), (7, 15))
