(* Test execution of user code in kernel mode. *)

operation add : int * int -> int

let adder =
  { add (a, b) -> exec (a + b) with { } } @ unit

let demo =
  using adder @ () run
    add (3, 4)
  with {
    return x @ _ -> x
  }