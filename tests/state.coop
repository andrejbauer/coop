operation get : unit -> int
operation put : int -> unit

let state =
  runner {
  | get t @ w -> (w, w)
  | put x @ w -> ((), x)
  } @ int

let demo =
  using state @ 1 run
    let a = get () in
    put 2 ;
    let b = get () in
    put 3 ;
    (a, b)
  finally {
    val x @ w -> (x, w)
  }
